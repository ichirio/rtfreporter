% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ard-experimental.R
\name{read_table_spec}
\alias{read_table_spec}
\title{Read an ARD table definition from a workbook}
\usage{
read_table_spec(path, output_id = NULL)
}
\arguments{
\item{path}{An \code{.xlsx} workbook (needs \pkg{readxl}) with the sheets
of \code{\link[=table_spec]{table_spec()}} (\code{study}, \code{tables}, \code{variables}, \code{cells}, \code{layout},
\code{columns}, \code{style}, \code{col_header}).  Any of them may be absent.  A definition is one file with several sheets,
so it is an Excel workbook and nothing else.}

\item{output_id}{The report to narrow the workbook to.  Rows with a blank
\code{output_id} are the study's defaults and stay; a row naming this
report replaces the default with the same key.  May be left \code{NULL}
for a workbook that defines one report; for several it must be given.}
}
\value{
An \code{\link[=table_spec]{table_spec()}}.
}
\description{
Read an ARD table definition from a workbook
}
\section{Lifecycle}{

\strong{Experimental.}  See \link{rtfreporter-ard}.
}

\seealso{
\code{\link[=table_spec]{table_spec()}}, \code{\link[=write_table_spec]{write_table_spec()}}
}

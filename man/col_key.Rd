% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/col_header.R
\name{col_key}
\alias{col_key}
\title{Select header columns by a column-name segment}
\usage{
col_key(key, sep = NULL, part = 1L)
}
\arguments{
\item{key}{Character. One or more segment values to match.}

\item{sep}{Single string, or \code{NULL} (default) to auto-detect the separator
that occurs in the column names (\code{"____"}, then tfrmt's delimiter).}

\item{part}{Integer. Which segment to compare, counting from the left
(\code{1}, the default, is the value before the first separator).  A negative
value counts from the right, so \code{part = -1} is the last segment.}
}
\value{
A function of the column names, tagged for use as \code{col_cell(pos =)}.
}
\description{
Returns a \strong{column selector} for \code{\link[=col_cell]{col_cell()}} \verb{pos =}: a function that is
handed the data column names when the header is attached to a table and
answers which columns the cell covers.
}
\details{
Data columns produced by a wide pivot are usually named by rule, as
\verb{<group><sep><sub-group>} -- for example \code{"Placebo____Day 1"}.  \code{col_key()}
splits each name on \code{sep} and keeps the columns whose \code{part}-th segment is
one of \code{key}, so a spanning header cell is written in terms of the \emph{value}
it labels rather than of column numbers that shift whenever a column is
added, dropped or reordered.

The separator is the one already used to rebuild a spanning header from
delimited column names (see \code{as_rtftables(header_sep = )}) and to split a
table column-wise (see \code{\link[=paginate_cols]{paginate_cols()}} \verb{by = }), so the three stay in
step.

A selector may also be written directly as a plain function of the column
names, which covers glob and regular-expression matching without any
further vocabulary:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{col_cell(function(nm) grepl(glob2rx("Placebo____*"), nm), "Placebo")
col_cell(function(nm) grepl("^Placebo____", nm),          "Placebo")
}\if{html}{\out{</div>}}

Such a function may return a logical vector the length of the column names,
an integer vector of positions, or a character vector of column names.  It
is an error for a selector to match no column, or to match columns that are
not adjacent -- a header cell can only span a contiguous range.
}
\examples{
# Data columns: Placebo____Day 1, Placebo____Day 7, TAK-003____Day 1, ...
col_cell(col_key("Placebo"), "Placebo\n(N=60)")

# Every "Day 1" column, whichever arm it belongs to:
col_cell(col_key("Day 1", part = 2), "Day 1")

}
\seealso{
\code{\link[=col_cell]{col_cell()}}, \code{\link[=rtf_col_header]{rtf_col_header()}}, \code{\link[=paginate_cols]{paginate_cols()}}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ard-experimental.R
\name{ard_keys}
\alias{ard_keys}
\title{What is actually inside an ARD}
\usage{
ard_keys(ard)
}
\arguments{
\item{ard}{A cards/cardx ARD (any data frame with the ARD columns).}
}
\value{
Invisibly, a list with elements \code{keys}, \code{variables}, \code{contexts} and
\code{stats} (a data frame of context / stat_name / stat_label).
}
\description{
Prints, and returns invisibly, the structural facts you need in order to
call \code{\link[=ard_table]{ard_table()}}: the grouping-variable names that appear in the
\code{group1..groupN} columns, the analysis variables, the \code{context} values and
the statistics each context carries.  All of it is read from the tibble's
rows -- never from the object's attributes.
}
\section{Lifecycle}{

\strong{Experimental.}  See \link{rtfreporter-ard}.
}

\seealso{
\code{\link[=ard_table]{ard_table()}}, \code{\link[=ard_template]{ard_template()}}
}

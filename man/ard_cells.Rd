% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ard-experimental.R
\name{ard_cells}
\alias{ard_cells}
\title{Several rows in one cell recipe, each with its own fallback chain}
\usage{
ard_cells(...)
}
\arguments{
\item{...}{One argument per output row.  Names become row labels; an
unnamed argument takes its label from \code{label}, as a bare template does.}
}
\value{
An object of class \code{ard_cells}, for \code{cells} in \code{\link[=ard_spread]{ard_spread()}}.
}
\description{
\code{cells} already reads three containers: one template is one row, a \emph{named}
character vector is one row per element, and a \code{list()} is a map looked up
by analysis variable.  The one shape those cannot spell is a \strong{named row
whose value is itself a chain} -- \code{c("1" = c(a, b))} is flattened by \code{c()}
before \code{ard_spread()} ever sees it.  \code{ard_cells()} is that shape, and only
that shape.
}
\details{
Each argument is one output row: its name is the row label, its value is a
template, a chain of templates, or a chain with guards.  Reading a recipe
then goes \code{list()} for \emph{which variable}, \code{ard_cells()} for \emph{which row},
\code{c()} for \emph{which template to try first}.
}
\section{Lifecycle}{

\strong{Experimental.}  See \link{rtfreporter-ard}.
}

\examples{
# an estimate line and a confidence-interval line, the first guarded so a
# count of zero prints as "0" rather than "0 (0.0)"
ard_cells(
  "1" = c(n == 0 ~ "0", "{n:.0f} ({estimate:.1f\%})"),
  "2" = "{conf.low:.1f\%}, {conf.high:.1f\%}")
}
\seealso{
\code{\link[=ard_spread]{ard_spread()}}, \link{rtfreporter-ard}
}

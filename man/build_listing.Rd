% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/listing.R
\name{build_listing}
\alias{build_listing}
\title{Reshape a data.frame into a listing body}
\usage{
build_listing(data, spec)
}
\arguments{
\item{data}{A \code{data.frame} (or tibble) of source data -- one row per record.
An rlistings \code{listing_df} is \strong{not} accepted: it has already been laid
out by rlistings, and goes straight to \code{\link[=as_rtftables]{as_rtftables()}}.}

\item{spec}{A \code{\link[=listing_spec]{listing_spec()}}.}
}
\value{
A \code{data.frame}: the printed columns in order, gutter columns between
them, and (unless \code{record = FALSE}) the hidden record column last.  It
carries the spec as the attribute \code{rtf_listing}, which \code{\link[=as_rtftables]{as_rtftables()}}
reads.
}
\description{
Turns source data into the data.frame a listing prints: source variables
joined into their printed columns, long cells broken over as many physical
rows as they need, gutter columns between the printed ones, a blank row
after each record, and a hidden record column so a page break never lands
inside a record.
}
\details{
This is preparation only -- the result is an ordinary data.frame, and
\code{\link[=as_rtftables]{as_rtftables()}} does the rendering.  The result carries the spec as an
attribute, so the two halves compose:

\preformatted{
  build_listing(adsl, spec) |> as_rtftables(max_rows = 40)
  as_rtftables(adsl, listing = spec, max_rows = 40)   # the same thing
}

The second form is the usual one; reach for this one to look at (or patch)
the reshaped data before it is rendered.
}
\section{How a cell wraps}{


Under the \code{"multiline"} type, a cell longer than its column's \code{width}
breaks \strong{after the separator} first, so each source variable starts its own
line; a piece that is still too long breaks again at a word boundary (after
a space, comma or hyphen); and a token that is \emph{still} too wide is
hard-split, so \strong{every line fits the column it was measured against}.  A
line break already in the data is honoured before any of this.

\code{width} is a \strong{display width}: a full-width (CJK) glyph occupies two
monospaced columns and is counted as two, so a Japanese listing wrapped to
20 really does fit in twenty.

Every column of one record is padded to the tallest, so the record's rows
stay aligned across columns.
}

\examples{
adsl <- data.frame(
  USUBJID = c("01-701-1015", "01-701-1023"),
  HIST    = c("ADENOCARCINOMA", "SQUAMOUS CELL CARCINOMA"),
  BRCA    = c("BRCA1", NA),
  ARM     = c("Placebo", "Xanomeline High Dose"),
  stringsAsFactors = FALSE
)

spec <- listing_spec(list(
  listing_col("USUBJID", width = 11, label = "Unique\nSubject ID"),
  listing_col(c("HIST", "BRCA"), width = 16,
              label = "Histology/\nMutation"),
  listing_col("ARM", width = 12, label = "Treatment Arm")
))

body <- build_listing(adsl, spec)
body

}
\seealso{
\code{\link[=listing_spec]{listing_spec()}} and \code{\link[=listing_col]{listing_col()}} for the settings;
\code{\link[=as_rtftables]{as_rtftables()}}, whose \code{listing} argument runs this inside the pipeline.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/col_header.R
\name{col_cell}
\alias{col_cell}
\title{Column-header cell specification}
\usage{
col_cell(
  pos,
  label = "",
  align = NULL,
  bold = FALSE,
  italic = FALSE,
  underline = FALSE,
  border = NULL
)
}
\arguments{
\item{pos}{Cell position, one of:
\itemize{
\item a numeric of length 1 (single column) or length 2 (\code{c(start, end)},
inclusive) -- \code{start <= end} required, values \verb{>= 1}; or
\item a character of length 1 (single column name) or length 2
(\code{c(start_name, end_name)}) referring to data columns by name.
Name resolution (and the \code{start <= end} check for named ranges) happens
when the header is attached to a table.
}}

\item{label}{Character scalar.  Cell text; may be \code{""}.}

\item{align}{Optional \code{"left"}, \code{"center"}, or \code{"right"}.  \code{NULL}
(default) inherits the leftmost covered column's \code{header_align}.}

\item{bold, italic, underline}{Logical.  Default \code{FALSE}.}

\item{border}{Optional \code{\link[=rtf_border]{rtf_border()}} applied to \strong{this header cell
only}, overriding the zone border and the automatic group-underline.
This is how you fine-tune individual rules in a multi-row header --
for example removing the bottom line under one spanning cell with
\code{border = rtf_border(bottom = rtf_border_side("none"))}, or adding a
thicker rule under one column.  \code{NULL} (default) inherits the normal
zone borders.}
}
\value{
A list of class \code{"rtf_col_cell"}.
}
\description{
Convenience constructor for a single cell in a column-header row passed
to \code{\link[=rtftable]{rtftable()}} (via \verb{col_header =}) or \code{\link[=rtf_col_header]{rtf_col_header()}}.
}
\details{
Use \code{pos = 1} for a single-column cell and \code{pos = c(start, end)} for a
cell that spans several data columns.  Positions are always relative to
the underlying data columns, not to the previous header row.

\code{pos} may instead be \strong{column name(s)} (character), resolved against the
data columns when the header is attached to a table.  This makes a
spanning cell robust to column reordering and errors out on an unknown
name -- for example \code{col_cell(c("g1", "g3"), "Drug A")} spans from the
column named \code{g1} to the one named \code{g3}, and \code{col_cell("total", "Total")}
targets a single named column.
}
\examples{
col_cell(1, "Item")
col_cell(c(2, 5), "Treatment", align = "center", underline = TRUE)

# Remove the group underline under just this spanning cell:
col_cell(c(2, 3), "Drug A",
         border = rtf_border(bottom = rtf_border_side("none")))

}

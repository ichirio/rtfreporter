% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ard-experimental.R
\name{ard_big_n}
\alias{ard_big_n}
\title{The big N: per-column statistics that belong in the column header}
\usage{
ard_big_n(ard, cols, stat = "N", variable = NULL, sep = "____", levels = NULL)
}
\arguments{
\item{ard}{A cards/cardx ARD.}

\item{cols}{The column key(s), named the same way as in \code{\link[=ard_table]{ard_table()}} --
the grouping variable's own name, not its \verb{group*} position.}

\item{stat}{Statistic to read.  \code{"N"} (the default) is the denominator.}

\item{variable}{Read the statistic from this analysis variable only.
\code{NULL} (default) prefers the by-variable's own count rows -- the
\verb{variable == <a }cols\verb{ key>} rows that \code{ard_stack(.by = )} writes -- and
falls back to the denominator carried on the summary rows.}

\item{sep}{Separator between multiple \code{cols} keys; match \code{\link[=ard_table]{ard_table()}}.}

\item{levels}{Optional level order for the keys, so the result lines up with
the table's columns.}
}
\value{
A named vector, one element per column key.
}
\description{
An ARD carries more than the table's body: the denominator behind every
percentage, the subject count per arm, the total N.  Those belong in the
\strong{column header} (\verb{Placebo\\\\nN = 86}), not in a row, so \code{\link[=ard_table]{ard_table()}} does
not put them anywhere.  \code{ard_big_n()} reads them out, keyed exactly like the
spread columns, ready to paste into a \code{col_header}.
}
\details{
Taking the denominator from the ARD rather than counting the data again is
the point: it is by construction the same number the percentages used.
}
\section{Lifecycle}{

\strong{Experimental.}  See \link{rtfreporter-ard}.
}

\examples{
if (requireNamespace("cards", quietly = TRUE)) {
  adsl <- cards::ADSL
  adsl$TRT <- as.character(adsl$ARM)
  ard <- cards::ard_stack(adsl, .by = TRT,
                          cards::ard_continuous(variables = AGE))
  n <- ard_big_n(ard, cols = "TRT")
  paste0(names(n), "\nN = ", n)
}
}
\seealso{
\code{\link[=ard_table]{ard_table()}}, \code{\link[=ard_keys]{ard_keys()}}
}

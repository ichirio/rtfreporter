% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ard-experimental.R
\name{read_report_spec}
\alias{read_report_spec}
\title{Read a report definition: the table and everything around it}
\usage{
read_report_spec(path, output_id = NULL)
}
\arguments{
\item{path}{One or more \code{.xlsx} workbooks.}

\item{output_id}{The report to narrow the definition to.}
}
\value{
A \code{\link[=table_spec]{table_spec()}} carrying the report sheets as well; it serves
\code{rtf_plan(spec = )} and \code{\link[=rtf_report]{rtf_report()}} alike.
}
\description{
A \strong{report definition} is the \code{\link[=table_spec]{table_spec()}} sheets plus the ones that
say how a report's pages are dressed:\tabular{lll}{
   sheet \tab one row per \tab holds \cr
   \code{report} \tab report \tab \code{type}, \code{file}, \code{program}, \code{auto_section}, font sizes \cr
   \code{page} \tab report \tab paper, orientation, margins, the document's text defaults \cr
   \code{header} \tab line of the page header \tab \code{line}, \code{left}, \code{center}, \code{right} \cr
   \code{footer} \tab line of the page footer \tab the same \cr
   \code{titles} \tab line above the table \tab the same \cr
   \code{footnotes} \tab line below the table \tab the same \cr
}


plus \code{study} keys \code{output_path} (where the RTF files go) and
\code{program_dir} (where the programs are, for \code{{PROGRAM}}).

The one \code{output_id} rule applies, and on the line sheets \strong{the line is
the key}: the study's page header is written once, as default lines 1
and 2, a report's titles follow on its own lines 3, 4, ..., and a
run-information line 99 in the default footer
(\verb{\{PROGRAM\}      Generated on: \{DATETIME\}}) closes every report's
footnotes.  A line with no text is a blank line.  The page tokens
(\code{{PAGE}}, \code{{TOTAL_PAGES}}, ...) and the run tokens (\code{\link[=generate_rtfreport]{generate_rtfreport()}})
work in every cell.

The sheets may be in \strong{one workbook or several} --- a \code{report.xlsx} a
lead keeps (the list of outputs, titles, footnotes) and a \code{tables.xlsx}
the programmers keep: give \code{path} as a vector and the sheets are read
together.  A sheet found in two of them is an error.
}
\section{\code{report}}{

\code{type} (\code{table}, \code{listing}, \code{figure}; default \code{table}), \code{file} (default
\verb{\{output_id\}.rtf}), \code{program} (default \code{{output_id}}, joined to
\code{study$program_dir} for \code{{PROGRAM}}), \code{auto_section}, \code{section_align},
\code{auto_title}, \code{title_align} (as \code{\link[=rtf_tables]{rtf_tables()}} takes them), and
\code{table_font_size}, \code{title_font_size}, \code{footnote_font_size} (half-points),
and \code{page_header} / \code{page_footer} (\code{FALSE} drops that running band for
the report, the study's default lines included).
}

\section{\code{page}}{

\code{paper_size}, \code{orientation}, \code{width_in}, \code{height_in}, \code{margin_top_in},
\code{margin_bottom_in}, \code{margin_left_in}, \code{margin_right_in},
\code{header_dist_in}, \code{footer_dist_in} (the page, as \code{\link[=rtf_document]{rtf_document()}} takes
it), and \code{font_size_half_points}, \code{title_format}, \code{footnote_format},
\code{title_width}, \code{footnote_width}, \code{markup} (\code{\link[=rtf_default_format]{rtf_default_format()}}).
}

\section{Lifecycle}{

\strong{Experimental.}  See \link{rtfreporter-ard}.
}

\seealso{
\code{\link[=rtf_report]{rtf_report()}}, \code{\link[=report_path]{report_path()}}, \code{\link[=read_table_spec]{read_table_spec()}}
}

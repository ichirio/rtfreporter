% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/listing.R
\name{listing_wrap}
\alias{listing_wrap}
\title{Break a listing cell or header into the lines it occupies}
\usage{
listing_wrap(text, width, sep = "/", layout = c("stack", "flow"))
}
\arguments{
\item{text}{The text to break.  A vector is wrapped element by element.}

\item{width}{Maximum display width per line, or \code{NULL} for no limit.}

\item{sep}{The separator to break after (default \code{"/"}).  \code{""} or \code{NULL}
skips step 1.}

\item{layout}{\code{"stack"} (default) breaks after \strong{every} separator;
\code{"flow"} treats it as a break opportunity and fills the line.  See
\code{\link[=listing_col]{listing_col()}}.}
}
\value{
For a length-1 \code{text}, a character vector of lines; for a longer
one, a list of such vectors.
}
\description{
The rule \code{build_listing()} applies to every cell, exposed so it can be used
on its own -- to preview where a column will break, or to lay a header out
by hand and hand the result to \code{listing_col(label = )}, which takes a
character vector as its lines.
}
\section{The rule}{

\enumerate{
\item \strong{After the separator.}  \code{"COMPLETED/BRCA1"} prefers to break between
its parts, and the separator stays at the end of the line it closes --
the look a stacked listing column is expected to have.
\item \strong{At a word boundary} -- after a space, a comma or a hyphen -- filling
each line as far as \code{width} allows.
\item \strong{Hard split}, for a token still too wide on its own.  Every line
returned therefore fits \code{width}, which is what lets the row count
describe the page (see \code{\link[=build_listing]{build_listing()}}).
}

\code{width} is a \strong{display width}: a full-width (CJK) glyph counts as two.  A
line break already in \code{text} is honoured before any of this.  With no
\code{width} there is nothing to break against and \code{text} is returned as it
stands.
}

\examples{
listing_wrap("COMPLETED/BRCA1/ADENOCARCINOMA", 22)

# A token with nowhere to break is split, so every line fits.
listing_wrap("Immunohistochemistry", 8)

# Lay a header out by hand, then hand the lines to listing_col().
listing_col("HIST", width = 16,
            label = listing_wrap("Histology of the tumour", 16))

# "flow" keeps short parts side by side.
listing_wrap("40/F", 20)
listing_wrap("40/F", 20, layout = "flow")

}
\seealso{
\code{\link[=listing_col]{listing_col()}}, whose \code{width} applies this to a column's cells and
whose \code{label} accepts the result as header lines.
}

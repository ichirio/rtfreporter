% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/listing.R
\name{listing_measures}
\alias{listing_measures}
\alias{listing_disp_width}
\alias{listing_take}
\alias{listing_split_after}
\title{The measurements a listing's wrapping rule is built on}
\usage{
listing_disp_width(x)

listing_take(x, width)

listing_split_after(text, sep)
}
\arguments{
\item{x, text}{Text to measure, cut or split.  \code{listing_disp_width()} is
vectorised; the other two take one string.}

\item{width}{Maximum display width.}

\item{sep}{The separator to break after.  \code{""} or \code{NULL} returns \code{text}
unsplit.}
}
\value{
\code{listing_disp_width()} an integer vector; \code{listing_take()} a single
string; \code{listing_split_after()} a character vector of pieces.
}
\description{
The three primitives \code{\link[=listing_wrap]{listing_wrap()}} uses to lay text out, exported so
that a rule of your own -- see \code{\link[=listing_wrap_code]{listing_wrap_code()}} -- shares them rather
than carrying a copy.  They are useful on their own too: a listing's
arithmetic is done in DISPLAY widths, where a full-width (CJK) glyph counts
as two, and \code{nchar()} alone would let a Japanese column ask for 20 columns
and take up to 40.
}
\details{
\code{listing_disp_width()} measures.  \code{listing_take()} cuts: the longest prefix
that still fits, never \code{""} for non-empty text, so a caller looping on the
remainder always makes progress even where one glyph is wider than the whole
column.  \code{listing_split_after()} breaks after each separator and keeps it at
the end of the piece it closes, which is the look a stacked listing column
is expected to have.
}
\examples{
listing_disp_width(c("ABC", "あいう"))   # 3 and 6
listing_take("ADENOCARCINOMA", 6)
listing_split_after("COMPLETED/BRCA1", "/")
}
\seealso{
\code{\link[=listing_wrap]{listing_wrap()}}, the rule these build; \code{\link[=listing_wrap_code]{listing_wrap_code()}} to
fork that rule.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/listing.R
\name{listing_wrap_code}
\alias{listing_wrap_code}
\title{The default wrapping rule, as source to edit}
\usage{
listing_wrap_code(name = "my_wrap")
}
\arguments{
\item{name}{Name for the entry function -- the one to pass as \code{wrap}.  The
helpers are prefixed with it.}
}
\value{
A character vector of source lines, classed so that printing it
writes the code out.
}
\description{
The code \code{\link[=listing_wrap]{listing_wrap()}} runs, written out ready to paste into a script
and change.  Use it when a custom \code{\link[=listing_spec]{listing_spec()}} \code{wrap} needs a
\emph{different} rule rather than an adjustment to this one -- when delegating
to \code{listing_wrap()} and fixing up its result will not do.
}
\details{
What comes back is a verbatim copy of the shipped rule, comments and all,
regenerated from it and checked against it by the test suite.  There is no
\verb{rtfreporter:::} in it, and the entry function already matches the \code{wrap}
contract described in \code{\link[=listing_spec]{listing_spec()}}.

What it hands out is the rule's POLICY -- three functions.  What they
measure with is exported instead: \code{\link[=listing_disp_width]{listing_disp_width()}}, \code{\link[=listing_take]{listing_take()}}
and \code{\link[=listing_split_after]{listing_split_after()}} are called, not copied, because every fork
would keep them verbatim.  Edit the policy; leave the measurements shared.

The two helpers are named after \code{name}, so two edited rules can live in
one script.
}
\examples{
# Print it, paste it, edit it.
listing_wrap_code("my_wrap")

# It runs as it stands: the emitted rule is the shipped one.
src <- listing_wrap_code("my_wrap")
env <- new.env(parent = globalenv())
eval(parse(text = src), envir = env)
identical(env$my_wrap("COMPLETED/BRCA1", 12, "/", "stack"),
          listing_wrap("COMPLETED/BRCA1", 12))
}
\seealso{
\code{\link[=listing_wrap]{listing_wrap()}} to delegate to the rule instead of editing it,
\code{\link[=listing_spec]{listing_spec()}} for the contract an edited rule still has to keep, and
\code{\link[=listing_code]{listing_code()}}, which writes a spec out the same way.
}

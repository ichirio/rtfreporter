% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/listing_fit.R
\name{listing_code}
\alias{listing_code}
\title{Print a listing spec as the code that would build it}
\usage{
listing_code(spec, name = NULL, indent = 2L)
}
\arguments{
\item{spec}{A \code{\link[=listing_spec]{listing_spec()}}.}

\item{name}{Name to assign the spec to, e.g. \code{"listing"} produces
\code{listing <- listing_spec(...)}.  \code{NULL} (default) writes the call alone.}

\item{indent}{Number of spaces the \code{listing_col()} calls are indented by
(default \code{2}).}
}
\value{
A character vector of source lines, one per line of code, with class
\code{rtf_listing_code} -- printing it shows the code ready to copy.
}
\description{
Turns a \code{\link[=listing_spec]{listing_spec()}} into the source you would have written by hand, so
a spec that came out of \code{\link[=fit_listing_widths]{fit_listing_widths()}} can be pasted into the
program and tuned there.  The measurement is a starting point; the code is
where the decisions get made and reviewed.
}
\details{
Only what differs from the listing's own defaults is written out, so the
result reads like something a person wrote rather than a dump of every
setting.

A spec straight from \code{\link[=fit_listing_widths]{fit_listing_widths()}} therefore comes out in full --
\code{width}, \code{rel_width} and \code{label} on every column -- because the fit wrote
all three down.  That is the point: it is a template to edit.
}
\examples{
spec <- listing_spec(list(
  listing_col("USUBJID", width = 15, label = "Unique\nSubject ID"),
  listing_col(c("AGE", "SEX"), width = 12, layout = "flow"),
  listing_col("STAGE", width = 9)
))

listing_code(spec)
listing_code(spec, name = "listing")

# The usual round trip: measure, print, paste, tune.
code <- listing_code(spec, name = "listing")
writeLines(code)

}
\seealso{
\code{\link[=fit_listing_widths]{fit_listing_widths()}}, which proposes the widths this writes out.
}

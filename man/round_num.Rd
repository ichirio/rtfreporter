% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/num_format.R
\name{round_num}
\alias{round_num}
\title{Round the way R rounds, or the way SAS rounds}
\usage{
round_num(x, digits = 0, rounding = NULL)
}
\arguments{
\item{x}{A numeric vector.}

\item{digits}{Decimal places. Default \code{0}.}

\item{rounding}{\code{"r"} (base R, half to even) or \code{"sas"} (half away from
zero).  \code{NULL}, the default, reads \code{getOption("rtfreporter.rounding")},
which is \code{"r"} unless the session or site set it.}
}
\value{
A numeric vector the same length as \code{x}.
}
\description{
The two disagree on an exact half.  Base R rounds a half to the \strong{even}
digit (\code{round(0.5)} is \code{0}, \code{round(2.5)} is \code{2}); SAS's \code{ROUND()} rounds
it \strong{away from zero} (\code{0.5} is \code{1}, \code{2.5} is \code{3}, \code{-0.5} is \code{-1}).
Every formatter in the package -- \code{\link[=fmt_signif]{fmt_signif()}}, \code{\link[=fmt_round]{fmt_round()}},
\code{\link[=fmt_numeric]{fmt_numeric()}}, \code{\link[=format_count_pct]{format_count_pct()}} -- rounds with this same rule, so
a study that has to match a SAS-produced table sets it \strong{once}:

\preformatted{
options(rtfreporter.rounding = "sas")
}

An explicit \verb{rounding =} on any call still wins over the option.

\code{"sas"} also absorbs a binary representation error the way SAS's own
fuzz does: \code{2.675} is stored as \code{2.67499999999999982}, which base R
rounds to \code{2.67} and SAS to \code{2.68}.
}
\examples{
round_num(c(0.5, 1.5, 2.5, -0.5))                    # 0 2 2 0
round_num(c(0.5, 1.5, 2.5, -0.5), rounding = "sas")  # 1 2 3 -1
round_num(2.675, 2, rounding = "sas")                # 2.68
}
\seealso{
\code{\link[=fmt_round]{fmt_round()}} for the same rounding as printed text.
}

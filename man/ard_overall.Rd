% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ard-experimental.R
\name{ard_overall}
\alias{ard_overall}
\title{Where the table's overall row comes from}
\usage{
ard_overall(label, from = NULL)
}
\arguments{
\item{label}{Text for the overall row, e.g. \code{"Any TEAE"}.  It is written
into the first \code{hierarchy} column, so the row sits at the top level
beside the other outermost rows.}

\item{from}{The analysis variable that block summarised -- the treatment
variable, usually.  \code{NULL} (default) means the cards sentinel.}
}
\value{
An object of class \code{ard_overall}, for \code{\link[=ard_normalize]{ard_normalize()}}'s
\code{overall} argument.  That argument also takes a bare string, which is
\code{ard_overall(label)}.
}
\description{
An adverse-events table opens with a "subjects with at least one event"
row, and where that row lives in the ARD depends on how the ARD was built.
\code{cards::ard_stack_hierarchical(over_variables = TRUE)} writes it as rows
whose variable is the sentinel \code{..ard_hierarchical_overall..}.  Build the
same table by summarising each level separately and binding the results,
and there is no sentinel: the overall block counts the \strong{treatment itself},
so the arm sits in \code{variable} / \code{variable_level} with no grouping pair at
all.  The two are indistinguishable from the shape of the ARD, so which one
this is has to be said.
}
\section{Lifecycle}{

\strong{Experimental.}  See \link{rtfreporter-ard}.
}

\examples{
ard_overall("Any TEAE")                      # the cards sentinel rows
ard_overall("Any TEAE", from = "TRT01P")     # a separately-built block
}
\seealso{
\code{\link[=ard_normalize]{ard_normalize()}}, \code{\link[=ard_spread]{ard_spread()}}
}

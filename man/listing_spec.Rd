% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/listing.R
\name{listing_spec}
\alias{listing_spec}
\title{Bundle the settings for one listing}
\usage{
listing_spec(
  cols,
  type = "multiline",
  sep = NULL,
  spacer = NULL,
  spacer_rel_width = NULL,
  blank_row = NULL,
  blank_row_first = NULL,
  align = NULL,
  layout = NULL,
  record = TRUE
)
}
\arguments{
\item{cols}{The printed columns, in order: a list of \code{\link[=listing_col]{listing_col()}}
objects.  A bare string (or a character vector) stands for
\code{listing_col()} on it, so \code{cols = c("USUBJID", "AGE")} means two
unwrapped columns.}

\item{type}{Listing template name; see \emph{Listing types}.  Default
\code{"multiline"}.}

\item{sep}{Default separator for a \code{\link[=listing_col]{listing_col()}} that does not set its
own.  \code{NULL} (default) takes the template's.}

\item{spacer}{Logical.  Insert a narrow blank column between each pair of
printed columns.  \code{NULL} (default) takes the template's.}

\item{spacer_rel_width}{Relative width of those gutter columns.  \code{NULL}
(default) takes the template's.}

\item{blank_row}{Logical.  End each record's block with a blank row, so one
subject is visibly separated from the next.  \code{NULL} (default) takes the
template's.}

\item{blank_row_first}{Logical.  Start each page with a blank row.  Passed
on as \code{as_rtftables(blank_row_first = )}, which is what makes it \emph{per
page} rather than once per listing.  \code{NULL} (default) takes the
template's.}

\item{align}{Default alignment for columns that do not set their own.
\code{NULL} (default) takes the template's.}

\item{record}{\code{TRUE} (default) appends the hidden record column under its
standard name, \code{FALSE} appends none, or a single string names it
yourself.  See \emph{The record column}.}
}
\value{
An object of class \code{rtf_listing_spec}.
}
\description{
Collects the columns of a listing and the settings that apply to all of
them, so that \code{\link[=build_listing]{build_listing()}} and \code{as_rtftables(listing = )} take a single
argument and read exactly the same object -- neither one carries a copy of
the other's argument list.
}
\section{Listing types}{


\code{type} names a template that supplies every default below, and any argument
you pass explicitly overrides it -- the same relationship
\code{rtftable(border = "tfl")} has with its preset.  One type ships:

\describe{
\item{\code{"multiline"}}{The layout a wide clinical listing usually wants: a
\code{"/"} separator, gutter columns between the printed ones, a blank row
after each record and one at the top of every page, everything left
aligned, and text wrapped at the separator first and at word boundaries
only where a piece is still too long.}
}

Adding a type later changes no signature: it is one entry in the internal
registry, and it brings its own wrapping rule with it.
}

\section{The record column}{


A listing wraps one source row over several physical rows, so a page break
must not land inside one.  \code{record} asks \code{\link[=build_listing]{build_listing()}} to append a
hidden column holding the source row number; \code{as_rtftables(listing = )} then
points \code{group_col} at it, splits with \code{"group_safe"} and lists it in
\code{drop_cols}, so it decides the page breaks and is never printed.  Set
\code{record = FALSE} only if you intend to paginate some other way.
}

\examples{
spec <- listing_spec(list(
  listing_col("USUBJID", width = 15, label = "Unique\nSubject ID"),
  listing_col(c("SEX", "AGE"), width = 12, label = "Sex/\nAge"),
  listing_col("ARM", width = 20, label = "Treatment Arm")
))
spec

# Bare names are columns too: two unwrapped columns, no gutters.
listing_spec(c("USUBJID", "ARM"), spacer = FALSE)

}
\seealso{
\code{\link[=listing_col]{listing_col()}} for one column; \code{\link[=build_listing]{build_listing()}}, which applies
this to a data.frame; \code{\link[=as_rtftables]{as_rtftables()}}, whose \code{listing} argument does the
same thing inside the rendering pipeline.
}

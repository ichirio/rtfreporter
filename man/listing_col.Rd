% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/listing.R
\name{listing_col}
\alias{listing_col}
\title{One printed column of a listing}
\usage{
listing_col(
  vars,
  sep = NULL,
  width = NULL,
  label = NULL,
  name = NULL,
  rel_width = NULL,
  align = NULL
)
}
\arguments{
\item{vars}{Character.  One or more source column names, joined with \code{sep}
in the order given.  Missing (\code{NA}) and empty values are skipped, so a
record missing its middle value does not print a doubled separator.}

\item{sep}{Separator for \code{vars}.  \code{NULL} (default) takes the listing's own
(\code{"/"} under the \code{"multiline"} type).}

\item{width}{Integer or \code{NULL}.  Maximum characters per physical row before
the cell wraps.  \code{NULL} (default) never wraps this column.}

\item{label}{Column header text.  \code{"\\n"} starts a further header row, as
everywhere else in rtfreporter.  \code{NULL} (default) prints an empty header.}

\item{name}{Output column name.  \code{NULL} (default) uses the first entry of
\code{vars}; \code{\link[=listing_spec]{listing_spec()}} makes the set unique if two columns collide.}

\item{rel_width}{Relative width of this column in the rendered table.
\code{NULL} (default) uses \code{width} when there is one, otherwise the longest
line of \code{label}.}

\item{align}{\code{"left"}, \code{"center"} or \code{"right"}.  \code{NULL} (default) takes the
listing's own (\code{"left"} under the \code{"multiline"} type).}
}
\value{
An object of class \code{rtf_listing_col}.
}
\description{
Describes a single column of a listing: which source variables it is built
from, how they are joined, how wide it may be before its text wraps onto a
further physical row, and what its header says.  A list of these is what
\code{\link[=listing_spec]{listing_spec()}} takes, and \code{\link[=build_listing]{build_listing()}} turns into a data.frame.
}
\details{
\code{vars} may name \strong{several} columns: their values are joined with \code{sep},
missing and empty values dropped, so a column reading
\code{"ADENOCARCINOMA/BRCA1/GRADE 3"} is written \code{listing_col(c("HIST", "BRCA", "HISTGRD"))} rather than pasted by hand upstream.

\code{width} is a number of \strong{characters}, not a rendered width: it decides
where the text of this column breaks onto another physical row, and so how
tall each record's block is.  It does not set the column's width in the
table -- that is \code{rel_width}, which defaults to \code{width} when you give one.
}
\examples{
# One source column, wrapped at 15 characters.
listing_col("USUBJID", width = 15, label = "Unique\nSubject ID")

# Three source columns in one printed column, joined with "/".
listing_col(c("DISPTPD", "BRCA", "HIST"), width = 22,
            label = "Primary Diagnosis/\nAny (BRCA) Mutations/\nHistology")

# No wrapping, and a header only.
listing_col("STAGE", label = "Stage at\nInitial\nDiagnosis")

}
\seealso{
\code{\link[=listing_spec]{listing_spec()}}, which collects these; \code{\link[=build_listing]{build_listing()}}, which
applies them.
}

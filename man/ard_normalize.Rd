% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ard-experimental.R
\name{ard_normalize}
\alias{ard_normalize}
\title{Flatten an ARD into an explicitly keyed long table}
\usage{
ard_normalize(
  ard,
  keys = NULL,
  hierarchy = character(),
  overall = NULL,
  drop_contexts = c("attributes", "total_n"),
  drop_key_variables = TRUE
)
}
\arguments{
\item{ard}{A cards/cardx ARD.}

\item{keys}{Character vector of grouping-variable names to materialise as
columns.  \code{NULL} (default) uses every name found in the \verb{group*} columns.
This reads the tibble's rows, not its attributes.}

\item{hierarchy}{Character vector naming a nested hierarchy, outermost
first -- e.g. \code{c("AEBODSYS", "AEDECOD")}.  For these variables the rows
where \verb{variable == <name>} are that level's own summary rows, and are
folded into the matching key column.}

\item{overall}{Label to give the \code{..ard_hierarchical_overall..} rows
produced by \code{cards::ard_stack_hierarchical(over_variables = TRUE)}, e.g.
\code{"Any TEAE"}.  They are placed on the first \code{hierarchy} column.  \code{NULL}
(default) leaves them alone.}

\item{drop_contexts}{\code{context} values to discard.  The default drops the
\code{attributes} rows (whose \code{stat} is a vector and cannot be flattened) and
the \code{total_n} row.}

\item{drop_key_variables}{When \code{TRUE} (default), drops the rows that merely
describe a key variable itself -- the \code{context == "tabulate"} counts of the
by-variable -- which no table cell uses.}
}
\value{
A data frame with one row per ARD statistic: the key columns, the
ARD's own \code{variable} / \code{variable_level} / \code{context} / \code{stat_name} /
\code{stat_label} / \code{stat} / \code{stat_fmt}, the structural classification \code{.kind}
(\code{"continuous"} / \code{"categorical"}, see \link{rtfreporter-ard}), \code{.label} (the
deepest non-missing hierarchy value, or \code{variable_level}), \code{.overall}, and
\code{.depth} --- 1 = outermost within a \code{hierarchy}, and \code{NA} when there is
none, since depth only means something inside one.
}
\description{
Step one of the ARD conversion: turn the \code{group1 / group1_level} \emph{positional}
pairs into columns named after the grouping variables, flatten every
list-column, attach the formatted statistic (\code{stat_fmt}), and -- when the ARD
is hierarchical -- fold each level's own summary rows into the right key
column and record the nesting depth.
}
\details{
The result is a plain data frame that you can keep manipulating with base R
or dplyr before handing it to \code{\link[=ard_spread]{ard_spread()}}.  That is the intended route
for anything \code{\link[=ard_table]{ard_table()}} does not do by itself: marginal totals, derived
rows, custom sorting.
}
\section{Working on the result before \code{\link[=ard_spread]{ard_spread()}}}{

The result is a plain data frame; reshaping it in between is the point of
the two-stage split.  Everything it needs is in its \strong{columns}, so
\code{dplyr::mutate()}, \code{filter()}, \code{bind_rows()} and base \code{[} are all safe.

Two conveniences ride on attributes, and only some manipulations carry
them across.  \code{dplyr::filter()}, \code{slice()}, \code{arrange()}, \code{bind_rows()} and
base \code{[} / \verb{$<-} keep them; \code{dplyr::mutate()} and \code{select()}, base
\code{transform()} and \code{subset()} drop them.  \strong{Neither is required:} losing
them leaves the label column a character vector instead of a factor (the
row order falls back to first appearance; \verb{levels =} fixes it for good),
and makes \code{notes} report only the discards from \code{\link[=ard_spread]{ard_spread()}}.  Values
are unaffected, and a manipulation that really does break the rows is
caught -- two values arriving in one cell is an error, not a silent
overwrite.

\code{mutate()} is the one to watch, because it is the natural verb for a
one-pipe \verb{ard_normalize() |> ... |> ard_spread()}.  Name the label
column's order in \verb{levels =} and nothing is lost.
}

\section{Factor levels}{

\pkg{cards} stores the level of a factor variable as a one-element factor,
so the flattening has to take the label: a level comes back as \code{"<65"}, not
as the integer code \code{1}.  The order the factor declared is kept too, on the
\code{"ard_factor_levels"} attribute, and \code{\link[=ard_spread]{ard_spread()}} uses it as that
variable's row order unless \code{levels} says otherwise.
}

\section{Lifecycle}{

\strong{Experimental.}  See \link{rtfreporter-ard}.
}

\seealso{
\code{\link[=ard_spread]{ard_spread()}}, \code{\link[=ard_table]{ard_table()}}, \code{\link[=ard_keys]{ard_keys()}}
}

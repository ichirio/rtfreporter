% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/watermark.R
\name{rtf_watermark}
\alias{rtf_watermark}
\title{Build a page watermark}
\usage{
rtf_watermark(
  text,
  font_size_half_points = 144L,
  color = "#C8C8C8",
  angle = -45,
  font = NULL,
  width_in = 6,
  height_in = 2
)
}
\arguments{
\item{text}{The watermark word. A zero-length or empty string means no
watermark.}

\item{font_size_half_points}{Size in half-points, as everywhere else in the
package (\code{144L} = 72 pt, the default).}

\item{color}{Fill colour as \code{"#RRGGBB"}. The default is a light grey that
stays readable behind body text.}

\item{angle}{Rotation in degrees, counter-clockwise negative. \code{-45} (the
default) is the conventional bottom-left to top-right diagonal.}

\item{font}{Font family. \code{NULL} (default) uses the document font.}

\item{width_in, height_in}{Size of the shape's bounding box in inches. The
defaults suit a single word on a Letter/A4 page; a long phrase needs a
wider box.}
}
\value{
An \code{rtf_watermark} object for \code{rtf_document(watermark = )}.
}
\description{
Builds the \code{watermark} setting for \code{\link[=rtf_document]{rtf_document()}} -- a diagonal word such
as \code{"DRAFT"} or \code{"CONFIDENTIAL"} drawn behind the page body, on every page.
}
\details{
The watermark is written into the section header, so it repeats per page and
stays scoped to its section: concatenating with \code{\link[=assemble_rtf]{assemble_rtf()}} cannot leak
one document's watermark into the next.  A section can override the
document-wide setting by passing \code{watermark} in its
\code{rtf_section(secinfo = )}, including \code{watermark = NA} to turn it off for
that section alone.
}
\examples{
rtf_watermark("DRAFT")

# Bigger, redder, horizontal:
rtf_watermark("DO NOT DISTRIBUTE", font_size_half_points = 96L,
              color = "#E8B4B4", angle = 0, width_in = 7)

}
\seealso{
\code{\link[=rtf_document]{rtf_document()}}, \code{\link[=rtf_section]{rtf_section()}}, \code{\link[=rtf_page]{rtf_page()}}.
}

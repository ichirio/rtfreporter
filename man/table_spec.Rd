% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ard-experimental.R
\name{table_spec}
\alias{table_spec}
\title{A workbook-shaped definition of how an ARD becomes a table}
\usage{
table_spec(
  tables = NULL,
  variables = NULL,
  cells = NULL,
  study = NULL,
  layout = NULL,
  columns = NULL,
  style = NULL,
  col_header = NULL,
  report = NULL,
  page = NULL,
  header = NULL,
  footer = NULL,
  titles = NULL,
  footnotes = NULL
)
}
\arguments{
\item{tables, variables, cells, layout, columns, style, col_header}{Data
frames with the columns above; missing columns are added as \code{NA}.  \code{tables} may instead be a named list of the
sheets, or an \code{table_spec} (returned as it is).}

\item{study}{The \code{study} sheet: a \code{key} / \code{value} frame, or a named
vector such as \code{c(rounding = "sas")}.}
}
\value{
An object of class \code{table_spec}: a list of the sheets' data
frames.
}
\description{
\code{table_spec()} validates the definition that \code{\link[=ard_spread]{ard_spread()}} accepts as
\verb{spec =}, and \code{\link[=read_table_spec]{read_table_spec()}} builds one from a workbook.  It holds
what would otherwise be repeated in every script --- which keys go
across and down, the display label and order of each variable, and the
template and digits of every row --- in \strong{one sheet per grain}, so
that each fact is written once:\tabular{lll}{
   sheet \tab one row per \tab holds \cr
   \code{study} \tab study fact \tab the rounding family (\code{key} / \code{value}) \cr
   \code{tables} \tab report \tab the roles and the table-wide options \cr
   \code{variables} \tab variable \tab display label, order, level order \cr
   \code{cells} \tab line of a cell \tab template, guard, digits \cr
   \code{layout} \tab report \tab pages, groups, blank rows, stub \cr
   \code{columns} \tab printed column \tab width, row title, decimal split, hidden \cr
   \code{style} \tab report \tab border, row heights, font \cr
   \code{col_header} \tab header cell \tab line, columns, span, text, borders \cr
}


The first four say how the ARD becomes a table data frame; the last
four how that becomes \code{rtftable} pages.  \code{\link[=rtf_plan]{rtf_plan()}} reads them all
(\code{rtf_plan(data, spec = )}), as the first layers of a plan, so a verb
written after it still wins.
}
\section{\code{study}}{

What is \strong{one for the whole study} by definition, as \code{key} / \code{value}
rows.  Today that is \code{rounding} --- \code{r} (half to even) or \code{sas} (half
away from zero), see \code{\link[=round_num]{round_num()}} --- so that no two tables of one
study can round differently.  Blank leaves it to
\code{getOption("rtfreporter.rounding")}.
}

\section{One rule on the other sheets}{

Every sheet but \code{study} starts with \code{output_id}.  \strong{Blank means a study-wide
default; a row naming the report replaces the default row with the same
key} --- the whole \code{tables} row for that report, the \code{variables} row
for that variable, the \code{cells} rows for that variable / context / row.
So one workbook can hold a house style and every report's own changes to
it.  \code{\link[=read_table_spec]{read_table_spec()}} narrows it to one report with \verb{output_id =}.

Explicit \code{\link[=ard_spread]{ard_spread()}} arguments win over the spec, and the spec wins
over the defaults.

Lists inside a cell are \code{|}-separated (\code{TR01AG1 | SEROSTAT}).  A column
called \code{note} is allowed on any sheet and never read; any other column a
sheet does not know is an error, so a mistyped header cannot become a
setting that silently never applies.
}

\section{\code{tables}}{

\describe{
\item{\code{cols}}{Column keys, outermost first: \code{TR01AG1 | SEROSTAT}.}
\item{\code{rows}}{Row keys, in output order.  \code{name = column} renames
(\code{group1 = AEBODSYS}); a quoted value is a constant heading
(\code{group1 = "Worst Post-Baseline Values"}).}
\item{\code{label}}{The row-label source, in the same notation
(\code{label = AEDECOD}).  Blank keeps \code{.label}; \code{NA} builds it to tell
rows apart and then drops it; \code{NULL} leaves it out.}
\item{\code{stats}, \code{value}, \code{sep}, \code{sort_stat}, \code{na}}{As the
\code{\link[=ard_spread]{ard_spread()}} arguments of the same name.}
\item{\code{sort}}{\code{TRUE}, \code{FALSE}, or the keys in order:
\code{.overall | group1 | .depth | -n | label}.}
}
}

\section{\code{variables}}{

\describe{
\item{\code{variable}}{An analysis variable, or any column key (\code{BASEGR},
\code{ATPT}, or the label column's own name).}
\item{\code{label}}{Display text replacing the variable's name.}
\item{\code{order}}{Number; the order the variables appear in.}
\item{\code{levels}}{The order of its values: \verb{Grade 0 | Grade 1 | Total}.}
}
}

\section{\code{cells}}{

\describe{
\item{\code{variable}, \code{context}}{What the row applies to.  Either may be
blank; both blank is the default for everything.  \code{variable} may
also be a context or kind (\code{continuous}, \code{categorical}), as a
\code{cells} map key may.}
\item{\code{row}}{The row label (\code{Mean (SD)}).  Blank means one row per
level, labelled by the level.}
\item{\code{when}}{Optional guard, ordinary R over the statistics:
\code{n == 0}.  \strong{Several rows with the same variable / context / row are
one chain}, tried in sheet order; the first whose guard holds and
whose template resolves wins.}
\item{\code{template}}{The cell recipe: \code{{mean} ({sd})}.}
\item{\code{digits}}{Decimal places for tokens that name none, per token
when comma-separated: \verb{1,2} for \code{{mean} ({sd})}.}
\item{\code{signif}}{Significant digits; wins over \code{digits}.}
}
For a \code{stats = rows} table (one statistic per row, the raw value in the
cell) a row with \strong{no template} is instead that statistic's display
format: \code{row} names the statistic as the label column prints it (\code{N},
\code{Mean}) and \code{digits} / \code{signif} say how many, as \code{\link[=fmt_numeric]{fmt_numeric()}} takes
them (\verb{fmt_numeric(by = <label column>, formats = )}).
}

\section{\code{layout}}{

One row per report, each column one argument of the plan verb its
prefix names:
\describe{
\item{\verb{pages_*}}{\code{\link[=plan_paginate_rows]{plan_paginate_rows()}}: \code{max_rows}, \code{split}, \code{by},
\code{min_group_rows}, \code{cont_label}.}
\item{\verb{group_*}}{\code{col}, \code{mode} and \code{collapse} of \code{\link[=plan_row_group]{plan_row_group()}};
\code{group_page = TRUE} is \code{\link[=plan_paginate_group]{plan_paginate_group()}}, one page per value
of \code{group_col}, and \code{group_show = FALSE} hides that column.}
\item{\verb{blank_*}}{\code{\link[=plan_blanks]{plan_blanks()}}: \code{where}, \code{first}, \code{last},
\code{counted}.}
\item{\verb{stub_*}}{\code{\link[=plan_stub]{plan_stub()}}: \code{vars}, \code{into}, \code{indent}, \code{summary},
\code{before}.}
\item{\verb{colpages_*}}{\code{\link[=plan_paginate_cols]{plan_paginate_cols()}}: \code{every}, \code{at}, \code{carry},
\code{order}.}
}
}

\section{\code{columns}}{

One row per printed column, by \strong{name} --- the finished table's, so
a folded stub is the name given to \code{stub_into}.  \code{.values} stands for
every spread column, however many the data turned out to have.
\describe{
\item{\code{width}}{Relative width.  Named columns win over \code{.values};
when widths are given, every printed column needs one.}
\item{\code{row_title}}{\code{TRUE} for a row-heading column.}
\item{\code{decimal_split}}{\code{TRUE} to line up the decimal points
(\code{\link[=set_decimal_split]{set_decimal_split()}}).}
\item{\code{hide}}{\code{TRUE} to use the column without printing it.}
}
}

\section{\code{style}}{

One row per report: \code{border}, \code{align_count_pct}, \code{auto_width},
\code{row_height_twips}, \code{header_row_height_twips},
\code{blank_row_height_twips}, \code{font}, \code{font_size_half_points},
\code{table_align}, \code{cell_valign}, as \code{\link[=rtftable]{rtftable()}} / \code{\link[=as_rtftables]{as_rtftables()}} take
them.

Values are checked where they are written: a number, \code{TRUE} / \code{FALSE}
or a \code{|}-list, as the column needs.  Quote a text value (\code{" (Cont.)"})
to keep its leading or trailing spaces.
}

\section{\code{col_header}}{

\strong{One row per header cell}; \code{line} 1 is the top row.  A report's own
cells replace the default header whole.
\describe{
\item{\code{cols}}{The columns the cell sits over, \code{|}-separated: a
column name, \code{.values} (every spread column), a position or range
(\code{3}, \code{3:31}, \code{3:last}), or \code{KEY = value} --- the spread columns
whose column key \code{KEY} has that value (\code{variable = n}).}
\item{\code{span}}{Blank: one cell over all of \code{cols}.  \code{each}: one cell
per column.  A column key (\code{TR01AG1}): one cell per value of that
key, over its columns --- an arm's spanner, however many arms.}
\item{\code{text}}{The label.  A line break is Alt+Enter or \verb{\\\\n}.  The
tokens of \code{\link[=plan_col_header]{plan_col_header()}} work: \code{{col}} (the column's own
value), \code{{col1}}, \code{{col2}} (its keys, outermost first), \code{{n}} (its
denominator) and \code{{n:sum}} (the total over the cell's columns).
\code{{n}} is read from the ARD whenever a text uses it.  Quote a text
to keep leading spaces: \code{"  Category"}.}
\item{\code{align}, \code{bold}, \code{border_top}, \code{border_bottom}}{As
\code{\link[=col_cell]{col_cell()}} / \code{\link[=rtf_border]{rtf_border()}} take them (\code{single}, \code{none}, ...).}
}
}

\section{Reserved for the rest of the report}{

A later version will read the sheets \code{titles}, \code{footnotes}, \code{page},
\code{header}, \code{footer} and \code{cell_styles} under the same \code{output_id} rule,
so the whole RTF deliverable can be defined in one workbook.  They are reported, not refused, when present today.  An
\code{about} sheet (\code{key} / \code{value}) may state \code{spec_version}; sheets whose
name starts with \verb{_} are ignored.
}

\section{Lifecycle}{

\strong{Experimental.}  See \link{rtfreporter-ard}.
}

\seealso{
\code{\link[=read_table_spec]{read_table_spec()}}, \code{\link[=write_table_spec]{write_table_spec()}}, \code{\link[=table_spec_template]{table_spec_template()}}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ard-experimental.R
\name{rtfreporter-ard}
\alias{rtfreporter-ard}
\alias{ard-experimental}
\title{Experimental: cards/cardx ARD to table data.frame}
\description{
A small family that turns an \strong{ARD} (Analysis Results Data, as produced by
\pkg{cards} and \pkg{cardx}) into the table \code{data.frame} that
\code{\link[=as_rtftables]{as_rtftables()}} consumes.  The target is \emph{one ARD record, one table row}.
Layouts that print one record over two lines, or that need derived rows such
as marginal totals, stay a human job -- do them on the returned data frame,
or on the long frame from \code{\link[=ard_normalize]{ard_normalize()}}.
}
\section{The functions}{

\describe{
\item{\code{\link[=ard_keys]{ard_keys()}}}{What keys, variables, contexts and statistics an ARD
actually holds.}
\item{\code{\link[=ard_normalize]{ard_normalize()}}}{ARD to a flat, explicitly keyed long table.}
\item{\code{\link[=ard_spread]{ard_spread()}}}{Long table to the wide table data.frame.}
\item{\code{\link[=ard_table]{ard_table()}}}{Both of the above in one call.}
\item{\code{\link[=ard_template]{ard_template()}}}{Emit runnable conversion code for a given ARD.}
\item{\code{\link[=ard_spec]{ard_spec()}}, \code{\link[=read_ard_spec]{read_ard_spec()}}, \code{\link[=write_ard_spec]{write_ard_spec()}},
\code{\link[=ard_spec_template]{ard_spec_template()}}}{The spreadsheet definition file.}
\item{\code{\link[=ard_round]{ard_round()}}}{SAS-style versus R-style rounding.}
}
}

\section{Nothing is read from the object's attributes}{

A cards ARD carries attributes as well as rows, but they are not a contract:
\code{attr(ard, "args")} lists \code{by} and \code{variables} in a different order per
generator and cannot tell them apart, it keeps only the \strong{first} operand's
value after \code{dplyr::bind_rows()}, and it is not updated when the ARD is
filtered.  The ARD's class survives \code{bind_rows()} with a differently shaped
ARD, so dispatching on it is no safer.  Every structural decision here comes
from an explicit argument instead; only the tibble's rows are inspected.
}

\section{The cell template grammar}{

A template is a string with \code{{...}} tokens naming statistics:
\tabular{ll}{
\code{{mean}}      \tab the value \pkg{cards} itself formatted (\code{fmt_fun}) \cr
\verb{\{mean:.1f\}}  \tab 1 decimal place, rounded per \code{round} \cr
\verb{\{p:.1f\\\%\}}   \tab multiplied by 100 first, then 1 decimal \cr
\verb{\{mean:.3s\}}  \tab 3 significant digits \cr
\code{{n:d}}       \tab integer \cr
\code{{n:raw}}     \tab the raw \code{stat}, \code{as.character()}, untouched
}
A template whose statistics are not all present yields \code{NA}, which is what
lets \code{c("{n} ({p})", "{n}")} act as a fallback chain.  A \strong{named} vector of
templates produces one table row per element, the name being the row label:
that is how \code{Min} and \code{Max} become a single \verb{Min, Max} line.
}

\section{How a \code{cells} entry is chosen (and why it survives a cards upgrade)}{

\code{context} is a \pkg{cards} implementation detail and it moves.
\code{ard_continuous()} stamps \code{"continuous"}, but its 0.9 rename
\code{ard_summary()} stamps \code{"summary"}; \code{ard_categorical()} stamps
\code{"categorical"}, but \code{ard_tabulate()} stamps \code{"tabulate"}.  Keying \code{cells}
on the context alone would tie your script to one \pkg{cards} generation and
produce \strong{no cells at all} against another.

So every variable is also classified from what its rows actually contain --
its \strong{kind}:
\describe{
\item{\code{"categorical"}}{the variable has levels to enumerate (some
\code{variable_level} is present): a factor, a dichotomous value of interest,
a hierarchy term.}
\item{\code{"continuous"}}{it does not -- one row per statistic of one numeric
variable.}
}
That reading is structural, so it is the same on every \pkg{cards} version,
past and future.  A \code{cells} entry is then matched in this order:
\enumerate{
\item the analysis variable's own name;
\item \code{context}, with the known spellings treated as equivalent;
\item the kind, likewise;
\item \code{"default"}.
}
Step 2 lets a context-specific entry win where you wrote one -- cardx's
\code{"proportion_ci"}, \code{"survival"}, \code{"stats_t_test"} and friends.  Step 3 is
what keeps \code{continuous} / \code{categorical} (or \code{summary} / \code{tabulate}, either
spelling) working when \pkg{cards} renames a verb again.

\code{\link[=ard_keys]{ard_keys()}} prints both: the contexts, which are version-specific, and the
kinds, which are not.
}

\section{Lifecycle}{

\strong{Experimental.}  These functions are newer than the rest of the package
and are not covered by its stability expectations: names and arguments may
change, and the family may be withdrawn.  Nothing else in \pkg{rtfreporter}
depends on them.
}

\seealso{
\code{\link[=as_rtftables]{as_rtftables()}}, \code{\link[=stub_cols]{stub_cols()}}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ard-experimental.R
\name{ard_pull}
\alias{ard_pull}
\title{Pull a statistic out of an ARD, keyed like the spread columns}
\usage{
ard_pull(
  ard,
  cols,
  stat = "N",
  variable = NULL,
  context = NULL,
  levels = NULL,
  sep = "____"
)
}
\arguments{
\item{ard}{A cards/cardx ARD.}

\item{cols}{The column key(s), named as in \code{\link[=ard_table]{ard_table()}} -- the grouping
variable's own name, not its \verb{group*} position.}

\item{stat}{Statistic to read; \code{"N"} by default.}

\item{variable, context}{Restrict to this analysis variable and/or this
\code{context}.  Use them when the error message says the choice is ambiguous,
or to ask for the key variable's own rows.}

\item{levels}{Optional level order for the keys, so the result lines up
with the table's columns.}

\item{sep}{Separator between multiple \code{cols} keys; match \code{\link[=ard_table]{ard_table()}}.}
}
\value{
A named vector, one element per column key.
}
\description{
An ARD carries more than the table's body: the denominator behind every
percentage, the subject count per arm, a total the author computed
themselves.  Those belong in the \strong{column header} (\verb{Placebo\\\\nN = 86}) or in
an overall row, not in a body cell, so \code{\link[=ard_table]{ard_table()}} puts them nowhere.
\code{ard_pull()} reads one out, keyed exactly like the spread columns --
\code{"Placebo____F"} for a crossed header -- ready to paste into a \code{col_header}.
}
\details{
Taking the number from the ARD rather than counting the data again is the
point: it is by construction the number the percentages used.
}
\section{Which N}{

There is no one place an ARD keeps "the N".  \code{cards::ard_stack(.by = TRT)}
writes the per-arm count as \code{n} on the by-variable's own rows and the
\strong{study} total as \code{N} on those same rows, while every summary row carries
its own denominator as \code{N}; \code{ard_total_n()} adds \code{..ard_total_n..}; and an
author may compute their own, as the \code{bigN} statistic in the solicited-AE
example on Discussion #473.  Guessing between them produces a plausible
wrong number in a column header, so this function does not guess:
\itemize{
\item \code{stat} names the statistic, and defaults to \code{"N"}.
\item The \strong{key variables' own tabulations are excluded} by default, because
there \code{N} is the study total rather than the column's denominator.  Set
\code{variable} to one of them to ask for those rows on purpose.
\item If what is left still offers more than one answer, the call \strong{fails with
the candidates listed}, so you can pin it with \code{variable} / \code{context}.
}
}

\section{Lifecycle}{

\strong{Experimental.}  See \link{rtfreporter-ard}.
}

\examples{
if (requireNamespace("cards", quietly = TRUE)) {
  adsl <- cards::ADSL
  adsl$TRT <- as.character(adsl$ARM)
  ard <- cards::ard_stack(adsl, .by = TRT,
                          cards::ard_continuous(variables = AGE))
  n <- ard_pull(ard, cols = "TRT")            # the denominator, per arm
  paste0(names(n), "\\\\nN = ", n)
}
}
\seealso{
\code{\link[=ard_keys]{ard_keys()}}, which lists every statistic an ARD carries;
\code{\link[=ard_table]{ard_table()}}
}

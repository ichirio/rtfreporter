% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ard-plan-spike.R
\name{apply_plan}
\alias{apply_plan}
\title{Run a plan, or look inside it (SPIKE)}
\usage{
apply_plan(plan, stage = c("table", "normalize", "args"))
}
\arguments{
\item{plan}{An \code{\link[=ard_plan]{ard_plan()}}.}

\item{stage}{How far to go.  \code{"table"} (default) returns the table
\code{data.frame}, the same object \code{\link[=ard_spread]{ard_spread()}} returns.  \code{"normalize"}
returns the long frame \code{\link[=ard_normalize]{ard_normalize()}} returns, which is where you
reach in with dplyr if you have to.  \code{"args"} returns the resolved
argument lists without running anything --- the call the plan amounts to.}
}
\value{
A data frame, or for \code{stage = "args"} a list of two argument lists.
}
\description{
Resolves an \code{\link[=ard_plan]{ard_plan()}}'s layers and runs the conversion.  \code{stage} stops
it early, so the same one pass answers "what does this do" and "what did it
do" --- there is no second code path that could disagree with the first.
}
\section{Lifecycle}{

\strong{Spike.}  See \code{\link[=ard_plan]{ard_plan()}}.
}

\examples{
if (requireNamespace("cards", quietly = TRUE)) {
  p <- ard_plan(cards::ard_stack(
         cards::ADSL, .by = ARM,
         cards::ard_continuous(variables = AGE))) |>
    plan_spread(cols = "ARM", rows = c(group = "variable")) |>
    plan_cells(continuous = c("Mean (SD)" = "{mean} ({sd})")) |>
    plan_digits(2) |>
    plan_digits(AGE = 0)

  str(apply_plan(p, "args")$spread$cells)   # AGE won
  apply_plan(p)
}
}
\seealso{
\code{\link[=ard_plan]{ard_plan()}}, \link{plan_verbs}
}

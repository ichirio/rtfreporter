% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/listing_fit.R
\name{fit_listing_widths}
\alias{fit_listing_widths}
\title{Propose the listing's column widths from the page and the data}
\usage{
fit_listing_widths(
  data,
  spec,
  page = NULL,
  font = "courier_new",
  size_half_points = 18L,
  total_width = NULL,
  min_width = 6L,
  probs = 0.9
)
}
\arguments{
\item{data}{The source data, as passed to \code{\link[=build_listing]{build_listing()}}.}

\item{spec}{A \code{\link[=listing_spec]{listing_spec()}}.}

\item{page}{Page settings as an \code{\link[=rtf_page]{rtf_page()}} object (or a plain list of the
same fields) -- the paper size, orientation and margins the listing will
be rendered on.  \code{NULL} (default) uses rtfreporter's own page defaults.
Ignored when \code{total_width} is given.}

\item{font, size_half_points}{The font the listing renders in, used to turn
the page's width into a number of characters.  Defaults match
\code{\link[=auto_col_widths]{auto_col_widths()}}.}

\item{total_width}{Character budget for the whole listing, gutters included.
\code{NULL} (default) computes it from \code{page}, \code{font} and \code{size_half_points}.
Give it directly when you know the budget and would rather not describe
the page.}

\item{min_width}{Integer.  The narrowest a fitted column may be (default
\code{6}).}

\item{probs}{Quantile of a column's cell widths taken as its demand
(default \code{0.9}).}
}
\value{
The \code{listing_spec}, with a \code{width} on every column.  It carries the
measurement as an attribute, which \code{print()} shows.
}
\description{
Fills in the \code{width} of every \code{\link[=listing_col]{listing_col()}} that does not set one, by
measuring what the column demands and fitting the demands into the width the
page actually leaves.  The result is the same \code{\link[=listing_spec]{listing_spec()}}, ready to
hand to \code{\link[=build_listing]{build_listing()}} or \code{as_rtftables(listing = )} -- or to
\code{\link[=listing_code]{listing_code()}}, which prints it as source to paste and tune.
}
\section{How a width is chosen}{


The page decides the budget.  The sheet's writable width (paper and
orientation minus the side margins) is divided by the width of one character
in \code{font} at \code{size_half_points}, which gives the number of characters the
listing has to spend.  The gutter columns are subtracted first -- they are
printed too -- and so is every \code{width} you set yourself, because those are
decisions, not proposals.

Each remaining column's \strong{demand} is the \code{probs} quantile of the display
widths of its composed cells, floored by the longest line of its header and
by \code{min_width}.  A quantile rather than the maximum: one unusually long
value should wrap, not push every other column narrow.  The demands are then
scaled to the budget and rounded.

Widths are display widths, so a full-width (CJK) glyph counts as two
throughout.
}

\examples{
adsl <- data.frame(
  USUBJID = c("01-701-1015", "01-701-1023"),
  HIST    = c("ADENOCARCINOMA", "SQUAMOUS CELL CARCINOMA OF THE LUNG"),
  BRCA    = c("BRCA1", NA),
  STAGE   = c("IIIB", "IV"),
  stringsAsFactors = FALSE
)

spec <- listing_spec(list(
  listing_col("USUBJID"),
  listing_col(c("HIST", "BRCA")),
  listing_col("STAGE")
))

# Landscape A4, half-inch margins, 8pt Courier.
fitted <- fit_listing_widths(
  adsl, spec,
  page = rtf_page(paper_size = "A4", orientation = "landscape",
                  margin_left_in = 0.5, margin_right_in = 0.5),
  size_half_points = 16L
)
fitted

# A width you set yourself is kept, and the rest fit around it.
spec2 <- listing_spec(list(
  listing_col("USUBJID", width = 12),
  listing_col(c("HIST", "BRCA")),
  listing_col("STAGE")
))
fit_listing_widths(adsl, spec2, total_width = 60)

}
\seealso{
\code{\link[=listing_code]{listing_code()}}, which turns the result into pasteable source;
\code{\link[=listing_col]{listing_col()}} for setting a width yourself; \code{\link[=auto_col_widths]{auto_col_widths()}}, the
same idea for a table's columns in twips.
}

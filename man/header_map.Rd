% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/col_header_values.R
\name{header_map}
\alias{header_map}
\title{Show the column header of every page, cell by cell}
\usage{
header_map(x)
}
\arguments{
\item{x}{An \code{\link[=rtftable]{rtftable()}} or a list of them (pages).}
}
\value{
A \code{data.frame} with one row per header cell: \code{page}, \code{name},
\code{group}, \code{rows}, \code{row} (which header row), \code{cell}, \code{from}, \code{to}, \code{text}.
A page with no column header contributes nothing.
}
\description{
One row per header cell of every page: which page it is on, the keys that
page was cut at, which columns the cell covers, and the text it ends up
with. It is the check for a header built from a template and a \code{values}
table -- the mapping is visible at a glance, and assertable in a test.

\if{html}{\out{<div class="sourceCode r">}}\preformatted{pages |> set_col_header(hdr, values = vals) |> header_map()
#>  page name              group   rows row cell from to text
#>  1    Period 1...1      Period 1  1    1    2    2    9 "Placebo\\n(N=120)..."
}\if{html}{\out{</div>}}
}
\examples{
df <- data.frame(Parameter = c("Mean", "SD"), A = c("1", "2"),
                 B = c("3", "4"), stringsAsFactors = FALSE)
tbl <- rtftable(df) |> set_col_header(c("Parameter", "Drug", "Placebo"))
header_map(tbl)
}
\seealso{
\code{\link[=set_col_header]{set_col_header()}}, whose \code{values} argument this checks.
}

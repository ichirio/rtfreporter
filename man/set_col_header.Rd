% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/style_verbs.R
\name{set_col_header}
\alias{set_col_header}
\alias{set_col_header.rtftable}
\alias{set_col_header.list}
\title{Set the whole column header of a finished table (final-table coordinates)}
\usage{
set_col_header(x, ...)

\method{set_col_header}{rtftable}(x, ..., values = NULL, by = NULL, align = NULL)

\method{set_col_header}{list}(x, ..., values = NULL, by = NULL)
}
\arguments{
\item{x}{An \code{\link[=rtftable]{rtftable()}}, or a list of them (pages from \code{\link[=as_rtftables]{as_rtftables()}}).}

\item{...}{The header rows, in render order (top first) -- each a character
vector (a label row, optionally named to place labels by column name) or a
list of \code{\link[=col_cell]{col_cell()}} cells (a spanning / cell row, whose \code{pos} may be
column names or final positions).  Alternatively a single pre-built
\code{\link[=rtf_col_header]{rtf_col_header()}} object.  Passing nothing clears the header.

A \strong{fully named} label row need not have one entry per column; an
unnamed one must.  An unknown name, or a row that is only partly named,
is an error either way.}

\item{values}{Optional table of \strong{per-page values} for the \code{{tokens}} in
the header, one row per page key: write the header once and let each page
take its own numbers.

\if{html}{\out{<div class="sourceCode r">}}\preformatted{hdr  <- rtf_col_header(
  list(col_cell(1L, ""),
       col_cell(c(2L, 9L), "Placebo\\n(N=\{n_pbo\})\\nn(\%)")),
  c("Parameter", visits))
vals <- data.frame(group = periods, n_pbo = c(120, 118, 238))
pages |> set_col_header(hdr, values = vals)
}\if{html}{\out{</div>}}

A token is \code{{name}}, \code{name} being a column of \code{values}; doubling a brace
prints it literally; the render-time tokens (\code{{PAGE}},
\code{{TOTAL_PAGES}}, \code{{DATE}},
\code{{BOOK_PAGE}}, \code{{AUTO_PAGE}}, \code{{AUTO_TOTAL_PAGES}}) are left for the
renderer, and any other unfilled token is an error. Every row of \code{values}
must be used by some page, and every page must find a row -- a mistyped
level is an error, not a silently wrong header. \code{\link[=header_map]{header_map()}} shows what
each page ended up with.}

\item{by}{Which page key \code{values} is matched on, named after the axis it
matches -- the same vocabulary \code{paginate_cols(page_order = )} uses. The
key column of \code{values} carries that name.
\describe{
\item{\code{"group"}}{(default) the value a \code{split = "by_value"} page was cut
for (\code{rtf_paginate_meta$page_group}); with no group axis it falls back
to the page's name without its \code{"...n"} tail, and says so once.}
\item{\code{"rows"}}{the page's \code{page_by} value.}
\item{\code{"name"}}{the page's name, \code{"...n"} included -- always unique, so
this addresses one page exactly.}
}
Several may be given (\code{by = c("group", "rows")}) to match on the
combination. The keys live in the page's metadata, so they survive
\code{drop_cols} and the column split.}

\item{align}{Optional column-header text alignment for the final columns:
\code{"left"}/\code{"center"}/\code{"right"} (applied to every column) or a character
vector of length \code{ncol} (one per printed column).  \code{NULL} (default) leaves
the current header alignment untouched.}
}
\value{
An object of the same shape as \code{x} (rtftable, or list of pages).
}
\description{
\code{set_col_header()} replaces the column header of an already-built
\code{\link[=rtftable]{rtftable()}} -- typically the output of \code{\link[=as_rtftables]{as_rtftables()}} -- resolving every
cell against the \strong{final, printed table}: the columns you actually see,
addressed by \strong{name} or by \strong{visible position} (\code{1} = first printed
column).  Because it runs on the finished table, there is no "intermediate"
column layout to reason about -- no hidden \code{drop_cols}, no \code{stub_vars}
bookkeeping, no position shifting.  This is the recommended way to attach a
multi-row / spanning header on top of an \code{as_rtftables()} pipeline; pair it
with \code{\link[=rtf_columns]{rtf_columns()}} to see the exact column names first.
}
\details{
Contrast with the \verb{col_header =} argument of \code{\link[=rtftable]{rtftable()}} / \code{\link[=as_rtftables]{as_rtftables()}},
whose positions refer to the source body \strong{before} \code{drop_cols} /
\code{stub_vars} are applied.  \code{set_col_header()} always speaks the final table's
coordinates.

Like the other post-hoc verbs it is an S3 generic with an \code{rtftable} method
and a \strong{list} method (every page of an \code{\link[=as_rtftables]{as_rtftables()}} result), and it
chains with the native pipe.

A label row that \strong{names every entry} is a \emph{patch}: it says which column
each label belongs to, so it may be shorter than the table and the columns
it does not mention keep their own name.

\if{html}{\out{<div class="sourceCode r">}}\preformatted{rtf_columns(tbl)                              # row_label  g1  g2  Total
tbl |> set_col_header(c(g1 = "Low", g2 = "High"))
#>                                            # row_label Low High Total
}\if{html}{\out{</div>}}

An \strong{unnamed} row is positional and must carry one label per printed
column -- the check that catches a header written for the whole table and
applied after \code{\link[=paginate_cols]{paginate_cols()}}.
}
\examples{
df <- data.frame(row_label = c("A", "B"),
                 g1 = 1:2, g2 = 3:4, Total = 5:6)
tbl <- rtftable(df)
tbl <- set_col_header(
  tbl,
  list(col_cell("row_label", ""), col_cell(c("g1", "g2"), "Treatment")),
  c(row_label = "Category", g1 = "Low", g2 = "High", Total = "Total")
)
}
\seealso{
\code{\link[=rtf_columns]{rtf_columns()}} to list the final column names; \code{\link[=rtf_col_header]{rtf_col_header()}}
/ \code{\link[=col_cell]{col_cell()}} to build header rows; \code{\link[=add_header_row]{add_header_row()}} to add a single
row; \code{\link[=style_header]{style_header()}} to restyle existing header cells.
}
